version: '3.8'

services:
  anprvision-front-staging:
    container_name: anprvision-front-staging
    build:
      context: ../..
      dockerfile: angular/Dockerfile
      args:
        ENVIRONMENT: staging
        API_URL: ${API_URL}
    restart: always
    env_file:
      - .env
    ports:
      - "${FRONT_PORT}:80"
    volumes:
      - ../../nginx.local.conf:/etc/nginx/conf.d/default.conf
    networks:
      - anpr-net-staging
    labels:
      - com.anprvision.environment=staging

networks:
  anpr-net-staging:
    external: true

