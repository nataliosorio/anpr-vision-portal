version: '3.8'

services:
  anprvision-front-qa:
    container_name: anprvision-front-qa
    build:
      context: ../..
      dockerfile: angular/Dockerfile
    restart: always
    env_file:
      - .env
    ports:
      - "${FRONT_PORT}:80"
    networks:
      - anprvision_network
    labels:
      - com.anprvision.environment=qa

networks:
  anprvision_network:
    external: true
